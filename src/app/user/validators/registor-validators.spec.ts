import { RegistorValidators } from './registor-validators';

describe('RegistorValidators', () => {
  it('should create an instance', () => {
    expect(new RegistorValidators()).toBeTruthy();
  });
});
